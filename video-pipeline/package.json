{
  "name": "video-pipeline",
  "private": true,
  "version": "0.1.0",
  "description": "Orchestration scaffolding for the AI video pipeline (see ARCHITECTURE.md). JS/TS-side assembly layer only; Python-based generators run as separate services.",
  "engines": { "node": ">=18" },
  "scripts": {
    "remotion:preview": "remotion preview project/timeline/remotion/index.ts",
    "remotion:render": "remotion render project/timeline/remotion/index.ts"
  },
  "dependencies": {
    "video-use": "*",
    "remotion": "*",
    "@remotion/cli": "*",
    "@remotion/renderer": "*",
    "ffmpeg-static": "*"
  }
}
